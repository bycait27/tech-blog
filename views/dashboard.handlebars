{{#if blogPosts.length}}
<section class="your-blog-posts">
            {{#each blogPosts as |blogPost|}}
    {{> dashboard-list}}
    {{/each}}
    {{else}}
   <p>No blog posts found.</p>
</section>
{{/if}}

<div class="new-post-container">
    <a href="/dashboard/new"><button type="submit" class="new-post">+ New Post</button></a>
</div>

