<section class="comments">
    {{#each this}}
    <h3 class="comment-header"></h3>
    <div class="comment-container">
        <p class="comment-class">{{content}}</p>
        <p class="author">--{{user.username}}, {{format_date date_created}}</p>
    </div>
    {{/each}}
</section>